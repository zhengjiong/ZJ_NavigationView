<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="AppTheme.Base">
        <!--NavigationBar(下方导航栏底色)背景顏色,v21以上的手機才可以設置-->
        <item name="android:navigationBarColor">?attr/colorPrimary</item>


        <!--<item name="android:windowContentTransitions">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <item name="android:windowSharedElementEnterTransition">@android:transition/move</item>
        <item name="android:windowSharedElementExitTransition">@android:transition/move</item>-->
    </style>


    <style name="NavigationViewOnStatusBarDemo" parent="AppTheme.Base">
        <!--NavigationBar(下方导航栏底色)背景顏色,v21以上的手機才可以設置-->
        <item name="android:navigationBarColor">?attr/colorPrimary</item>

        <!--頂部状态栏颜色-->
        <!--<item name="colorPrimaryDark">@android:color/holo_green_light</item>-->

        <!--
        windowDrawsSystemBarBackgrounds系统将在你的window里面绘制status bar，
        默认为TRUE，之所以要写出来是因为你的theme有可能是继承过来的，确保为true
        -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <!--
        statusBarColor设置为透明是因为我们不再需要系统的status bar，
        因为我们无法控制它的位置，我们将交由DrawerLayout来处理。
        這裡必須設置成透明色,不然drawer會被系統的statusbar遮擋住
        -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!--<item name="android:statusBarColor">?colorPrimaryDark</item>--><!--drawer會被statusbar遮擋住-->
    </style>
</resources>
